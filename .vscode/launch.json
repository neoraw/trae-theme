{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Trae Neo Theme",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"]
    }
  ]
}
